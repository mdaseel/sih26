# ============================================================
#  SolarGrid AI — Python backend dependencies
#
#  Python 3.12 is required. suryagrid_model_v2.pkl was serialised by
#  scikit-learn 1.3.2, which requires numpy < 2; pandapower 3.4.0 is
#  the newest release that resolves against numpy 1.26.x.
# ============================================================

# --- Web framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.2
# Multipart form parsing — required by the vendor document-upload endpoint.
# Without it the app crashes at import (FastAPI raises on Form/File routes).
python-multipart==0.0.32

# --- Data validation & settings ---
pydantic==2.11.4
pydantic-settings==2.9.1

# --- Supabase / HTTP ---
supabase==2.15.2
httpx==0.28.1

# --- Power-flow simulation ---
# 3.4.0 is the version feeder_network.json was VALIDATED against and that the
# DOCS prescribe. 2.14 uses const_z_percent (unified ZIP); 3.x uses
# const_z/p/i_p/q split. A 2.14 build loading a 3.4 network errors on
# 'const_z_percent' — that was the prod /api/twin 422 you just hit on Render.
pandapower==3.4.0
networkx==3.4.2
scipy==1.13.1

# --- Data / ML ---
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.3.2

# --- Database migrations (scripts only) ---
psycopg[binary]==3.2.6
